import { StyledContainer, StyledParagraph, Icon } from "./styled";

const LoadingComponent = () => (
  <StyledContainer>
    <StyledParagraph>Please wait, projects are being loaded...</StyledParagraph>
    <Icon />
  </StyledContainer>
);

export default LoadingComponent;
